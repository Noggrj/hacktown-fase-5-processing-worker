apiVersion: v1
kind: Service
metadata:
  name: fiapx-worker
  namespace: fiapx
spec:
  type: ClusterIP
  selector:
    app: fiapx-worker
  ports:
    - name: http
      port: 80
      targetPort: 8080
